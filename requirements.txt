# =====================================================================
# AI Text Summarization Pro — Dependencies
# =====================================================================

# ── Web Framework & Server ────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
flask-sqlalchemy>=3.1.0
flask-login>=0.6.3
flask-bcrypt>=1.0.1
python-dotenv>=1.0.0
gunicorn>=21.2.0

# ── NLP / Summarization ────────────────────────────────────────────
sumy>=0.11.0
nltk>=3.8.1
textstat>=0.7.3
textblob>=0.18.0
vaderSentiment>=3.3.2
rake-nltk>=1.0.6
scikit-learn>=1.4.0

# ──# HuggingFace Transformers
# NOTE: torch is installed separately via render.yaml build command
# using --index-url https://download.pytorch.org/whl/cpu (CPU-only, ~250 MB)
# to avoid pulling the full CUDA build (~2.5 GB) on cloud servers.
transformers>=4.38.0

# ── File & Web Handling ───────────────────────────────────────────
PyMuPDF>=1.24.0
python-docx>=1.1.0
beautifulsoup4>=4.12.0
requests>=2.31.0
chardet>=5.2.0

# ── PDF Generation ────────────────────────────────────────────────
fpdf2>=2.7.9

# ── Utilities ─────────────────────────────────────────────────────
Werkzeug>=3.0.0